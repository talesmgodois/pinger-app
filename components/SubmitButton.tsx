import React from "react";
import Button from "@material-ui/core/Button";
import {
  createMuiTheme,
  withStyles,
  makeStyles,
  ThemeProvider,
} from "@material-ui/core/styles";

export const ColorButton = () => {
  return (
    <button type="button" data-framer-highlight="true" style="">
      Ping
    </button>
  );
};
